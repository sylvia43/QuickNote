<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.shreyasr.quicknote"
    android:versionCode="2"
    android:versionName="1.0.0" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name="me.shreyasr.quicknote.ApplicationWrapper"
        android:theme="@android:style/Theme.Translucent.NoTitleBar"
        android:backupAgent=".NoteBackupAgent" >
        <meta-data android:name="com.google.android.backup.api_key"
                   android:value="AEdPqrEAAAAIEWK6Kr-mlHHitMxz8zsaDWlHxXjyvI1eFT1JzA" />
        <activity
            android:name="me.shreyasr.quicknote.LauncherActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="me.shreyasr.quicknote.NotepadWindow" >
        </service>
        <service android:name="me.shreyasr.quicknote.PreferencesPopup" >
        </service>
        <service android:name="me.shreyasr.quicknote.InfoPopup" >
        </service>
    </application>

</manifest>
